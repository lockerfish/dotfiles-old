Installation:

	git clone git://github.com/lockerfish/dotfiles.git ~/.dotfiles

Neovim
	mkdir -p ~/.config
  ln -s ~/.dotfiles/vim ~/.config/nvim

Tmux
	ln -s ~/.dotfiles/tmux/.tmux.conf ~/.tmux.conf


sample dotfiles:
https://github.com/wwwdata/vim-config
